<!doctype html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>동화책 생성</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
          crossorigin="anonymous">

    <style>
        html, body {
            height: 100%;
            margin: 0;
            background-color: #000;
            color: #fff;
            overflow: hidden;
        }

        .container-wrapper {
            display: flex;
            height: calc(100% - 56px); /* header 제외한 높이 */
        }

        .sidebar {
            width: 250px;
            background-color: #ccc;
            padding: 10px;
            overflow-y: auto;
        }

        .story-list-item {
            background-color: white;
            color: black;
            border-radius: 6px;
            padding: 8px;
            margin-bottom: 10px;
            cursor: pointer;
        }

        .story-list-item:hover {
            background-color: #e2e2e2;
        }

        .main {
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            background-color: #111;
            color: white;
        }

        .chat-area {
            flex-grow: 1;
            overflow-y: auto;
            padding: 20px;
        }

        .chat-box {
            background-color: #222;
            border: 1px solid #555;
            border-radius: 8px;
            padding: 10px;
            margin-bottom: 10px;
        }

        .input-area {
            display: flex;
            padding: 10px;
            background-color: #000;
        }

        .input-area input {
            flex-grow: 1;
            padding: 10px;
            font-size: 16px;
        }

        .input-area button {
            margin-left: 10px;
            padding: 10px 16px;
        }
    </style>
</head>
<body>

<!-- 상단 네비게이션 -->
<nav class="navbar navbar-dark bg-dark">
    <div class="container-fluid">
        <div class="d-flex align-items-center">
            <span class="navbar-brand mb-0 h1">동화책 생성</span>
            <a href="/api/story/home" class="btn btn-outline-light btn-sm ms-2">🏠 홈</a>
        </div>
    </div>
</nav>

<!-- 알림 메시지 -->
{{#msg}}
    <div class="alert alert-primary alert-dismissible fade show" role="alert">
        {{msg}}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
{{/msg}}

<!-- 전체 컨텐츠 영역 시작 -->
<div class="container-wrapper">

    <!-- 📚 왼쪽 고정 사이드바: 모든 페이지 공통 적용 -->
    <div class="sidebar">
        <h4 class="mb-3">📚 내 동화책</h4>

        {{#stories}}
            <div class="story-list-item"
                 onclick="location.href='/api/story/{{storyId}}'">
                {{title}}
            </div>
        {{/stories}}
        {{^stories}}
            <div class="text-muted">동화책이 없습니다.</div>
        {{/stories}}
    </div>
